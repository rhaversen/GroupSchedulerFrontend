import EventsPage from '@/components/EventsPage'

export default function ParticipatingPage () {
	return (
		<EventsPage
			title="Events I'm Participating In"
			description="All events where you're involved - as creator, admin, or participant."
			mode="participating"
		/>
	)
}
